'use strict';

// define all the global variables that are uses to hold FC state
var OSD_VIDEO_CONFIG;

var OSD = {
    resetState: function() {
        OSD_VIDEO_CONFIG = {
            video_mode: 0,
        };        
    }
};